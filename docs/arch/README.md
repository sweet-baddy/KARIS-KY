# Architecture Documentation

## Overview

This directory contains **auto-generated visual architecture documentation** derived from the karis-ky escrow contract source code. The documentation includes entity relationships, data flow diagrams, state machines, and detailed reference tables.

**Generated by:** `scripts/generate_architecture_docs.py`

---

## Quick Start

### For first-time readers:
1. Start with [ARCHITECTURE.md](ARCHITECTURE.md) for the overview
2. Read [state-machine.md](state-machine.md) to understand escrow lifecycle
3. Review [data-flow.md](data-flow.md) to see who calls what
4. Dive into [storage-reference.md](storage-reference.md) for implementation details

### For integrators:
- [entrypoint-matrix.md](entrypoint-matrix.md) — which roles call which functions
- [data-flow.md](data-flow.md) — how tokens and auth flow through the contract
- [storage-reference.md](storage-reference.md) — persistent vs. instance keys

### For auditors:
- [entity-relationships.md](entity-relationships.md) — data model structure
- [module-structure.md](module-structure.md) — code organization and boundaries
- [ARCHITECTURE.md](ARCHITECTURE.md) — metrics and architecture decisions

---

## Document Guide

| Document | Purpose | Audience |
|----------|---------|----------|
| **[ARCHITECTURE.md](ARCHITECTURE.md)** | Overview, key metrics, ADR index | Everyone |
| **[state-machine.md](state-machine.md)** | Status transitions (0–4), legal hold impact | Integrators, Auditors |
| **[data-flow.md](data-flow.md)** | Authorization, storage, token, event paths | Integrators, Developers |
| **[entity-relationships.md](entity-relationships.md)** | Data model structure and associations | Auditors, DB designers |
| **[module-structure.md](module-structure.md)** | Code organization (lib.rs, external_calls.rs, validation.rs) | Developers |
| **[entrypoint-matrix.md](entrypoint-matrix.md)** | Role-based API surface (admin, SME, investor, treasury) | Integrators |
| **[storage-reference.md](storage-reference.md)** | Detailed `DataKey` and type catalog | Developers, Auditors |
| **[sharding-architecture.md](sharding-architecture.md)** | Investor storage sharding for 10k+ investor escrows | Scalability engineers |
| **[INTEGRATION_GUIDE.md](INTEGRATION_GUIDE.md)** | Off-chain integration patterns and best practices | Integrators |

---

## Sharding Architecture

### For Large-Scale Escrows (10k+ Investors)

The karis-ky contract supports **optional investor storage sharding** to scale beyond typical Soroban instance storage limits. Instead of storing all per-investor data in the primary contract's instance storage, investor contributions, yields, and claims are delegated to **shard contracts** spawned on-demand.

**Key files:**
- **[sharding-architecture.md](sharding-architecture.md)** — Complete sharding design, routing strategy, spawning, aggregation, and integration
- **[plantuml/sharding-architecture.puml](plantuml/sharding-architecture.puml)** — Component architecture diagram
- **[plantuml/sharding-flows.puml](plantuml/sharding-flows.puml)** — Sequence diagrams for fund, settle, and claim flows with sharding
- **`escrow/src/sharding.rs`** — Sharding module implementation

**Quick reference:**

| Aspect | Design |
|--------|--------|
| **Routing** | Deterministic hash: `shard_id = hash(investor) % shard_count` |
| **Spawning** | Lazy, on-demand when first investor routes to a shard |
| **Storage** | Investor data in persistent storage (per-address TTL) |
| **Aggregation** | Settlement queries all shards and verifies aggregate total == primary funded_amount |
| **Max shards** | Configurable at init (typically 256 or 1024) |
| **Backward compatible** | Single-shard escrows operate identically; sharding is optional |

**When to use sharding:**
- Escrow with 10,000+ unique investors
- Instance storage approaching limits
- Need for unbounded investor cardinality

**Typical architecture:**
```
Primary Escrow (slim state)
  ├─ Sharding config
  ├─ Shard registry (shard_id → address)
  ├─ Aggregate totals (funded_amount, unique_funder_count)
  └─ FundingCloseSnapshot (immutable)

Shard 0 ─ Investors 0, 2, 5, ...  (hash(addr) % N == 0)
Shard 1 ─ Investors 1, 3, 6, ...  (hash(addr) % N == 1)
...
Shard N ─ Investors M, M+2, ...   (hash(addr) % N == N)
```

---

## Key Concepts

### Escrow Status (State Machine)

```
OPEN (0) ─fund→ FUNDED (1) ─settle→ SETTLED (2) ─withdraw→ WITHDRAWN (3) → [Terminal]
  ↓                                                                              ↑
  └─────── cancel ─→ CANCELLED (4) ─refund→ [Investors reclaim] ──────────────┘
```

- **OPEN**: Accepting investments; no funding target met yet
- **FUNDED**: Target reached; awaiting maturity and settlement
- **SETTLED**: Settlement finalized; awaiting SME withdrawal
- **WITHDRAWN**: SME pulled liquidity; investors claim payouts
- **CANCELLED**: Admin halt; investors refund principal

### Storage Model

**Instance keys** (atomic, contract-owned):
- `DataKey::Escrow` — Full state snapshot
- `DataKey::Version` — Schema version
- `DataKey::LegalHold` — Compliance gate

**Persistent keys** (per-investor, bounded):
- `DataKey::InvestorContribution(addr)` — Principal balance
- `DataKey::InvestorEffectiveYield(addr)` — Chosen yield tier
- `DataKey::InvestorClaimNotBefore(addr)` — Claim lock timestamp
- `DataKey::InvestorClaimed(addr)` — Claim flag (prevent double-claim)

### Authorization Model

| Role | Entrypoints | Auth Gate |
|------|------------|-----------|
| **Admin** | init, propose_admin, set_legal_hold, bind_attestation | `admin` address |
| **SME** | withdraw, settle, record_collateral | `sme_address` |
| **Investor** | fund, fund_with_commitment, claim_investor_payout | caller address |
| **Treasury** | sweep_terminal_dust | `treasury` address |
| **Public** | get_escrow, get_version | none |

---

## Regenerating Docs

To update architecture docs after code changes:

```bash
cd /workspaces/KARIS-KY
python3 scripts/generate_architecture_docs.py --output-dir docs/arch
```

**What it extracts:**
- DataKey enum variants + descriptions
- Contract type structs (InvoiceEscrow, SmeCollateralCommitment, etc.)
- Public entrypoints (name, auth requirements, return types)
- State transitions (from ADR-001 model)

**Current coverage:**
- ✓ DataKey catalog (29 variants)
- ✓ Contract types (6 structs)
- ✓ Entrypoint matrix (67 public functions)
- ✓ State machine (5 states, hardcoded transitions)
- ✓ Module graph (lib.rs, external_calls.rs, validation.rs)

---

## Design Principles

### 1. State Machine (Immutable Forward Transitions)
Every escrow transitions through a fixed sequence of states. No backward steps or re-entry to earlier states. Legal hold can **block** transitions but not permit backward movement.

### 2. Authorization Boundaries (Per-Role)
Every state-mutating entrypoint follows:
1. Read-only preconditions (legal hold, status checks)
2. `Address::require_auth()` for the bound role
3. Storage writes + token transfers

### 3. Bounded Per-Investor Storage
Persistent storage entries (contribution, yield, claim lock) are keyed by investor `Address`. Instance storage is bounded by the escrow-specific `max_unique_investors` cap.

### 4. Immutable Bindings
Once set at initialization, these cannot change:
- Funding token contract
- Treasury address
- Yield tier table
- Maturity timestamp

### 5. Storage Evolution (ADR-007)
Schema version 6 moved per-investor keys to persistent storage to avoid instance-bloat as investor cardinality grows.

---

## Related Documentation

For context on design decisions and operational procedures:

- **[docs/adr/](../adr/)** — Architecture Decision Records (ADR-001 through ADR-008)
- **[docs/OPERATOR_RUNBOOK.md](../OPERATOR_RUNBOOK.md)** — Deployment, migration, and rollback procedures
- **[docs/escrow-data-model.md](../escrow-data-model.md)** — Detailed data model explanation
- **[docs/escrow-security-checklist.md](../escrow-security-checklist.md)** — Security review checklist
- **[escrow/README.md](../../escrow/README.md)** — Contract development guide

---

## Maintenance

**When to regenerate:**
- After adding new entrypoints
- After adding DataKey variants
- After changing struct fields
- Before major releases

**What's manually maintained:**
- Descriptions in enum/struct doc comments
- Authorization role tags in doc comments
- ADR references in ARCHITECTURE.md

**What's auto-extracted:**
- Entrypoint lists and signatures
- DataKey catalog
- Struct field mappings
- State transition diagram

---

## License

MIT (see [LICENSE](../../LICENSE) in root)
